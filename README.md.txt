General Noted and commands 


command to specificly install version of ruby with rvm in command line. 
REASON ADDED: rvm on ubunto was not cooperating with standard ruby install but this command worked
rvm install 3.1.3  

NOTE: sets default ruby to specified ruby version
rvm --default ruby-3.1.3

NOTE: if turbo rails single page features not working 
rails turbo:install
rails turbo:install:redis 